{
  "name": "Python Codespace",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": " sudo apt update && sudo apt install -y git-lfs && git lfs install && pip install -r requirements.txt"
}